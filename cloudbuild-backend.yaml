steps:
  # Step 1: Simpan secret ke file /secrets/DOTENV
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: "bash"
    secretEnv: ["BACKEND_ENV"]
    args:
      - "-c"
      - |
        mkdir -p /secrets
        echo "$BACKEND_ENV" > /secrets/DOTENV

  # Step 2: Build image dari backend
  - name: "gcr.io/cloud-builders/docker"
    dir: "backend"
    args: ["build", "-t", "gcr.io/$PROJECT_ID/tcc-be", "."]

  # Step 3: Push image ke registry
  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "gcr.io/$PROJECT_ID/tcc-be"]

  # Step 4: Deploy ke Cloud Run
  - name: "gcr.io/cloud-builders/gcloud"
    args:
      [
        "run", "deploy", "be",
        "--image", "gcr.io/$PROJECT_ID/tcc-be",
        "--timeout", "1000s",
        "--port", "5000",
        "--region", "us-central1",
        "--allow-unauthenticated"
      ]

availableSecrets:
  secretManager:
    - versionName: projects/$PROJECT_ID/secrets/backend-env/versions/latest
      env: BACKEND_ENV

options:
  logging: CLOUD_LOGGING_ONLY

timeout: "1600s"
